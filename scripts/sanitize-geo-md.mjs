/**
 * ลดถ้อยคำ meta/negative ในหน้า GEO (content/locations) — รัน: node scripts/sanitize-geo-md.mjs
 */
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const locDir = path.join(__dirname, '..', 'src', 'content', 'locations');

const pairs = [
  [
    /โดยไม่ต้องอ้างว่ามีทีมรับถึงที่ในทุกอำเภอ/g,
    'สอบถามพื้นที่ให้บริการและวิธีนัดรับ/ส่งเครื่องทางไลน์ @webuy ได้ก่อนเดินทาง',
  ],
  [
    /โดยไม่อ้างว่ามีทีมรับถึงที่ในทุกอำเภอ/g,
    'สอบถามพื้นที่ให้บริการและวิธีนัดรับ/ส่งเครื่องทางไลน์ @webuy ได้ก่อนเดินทาง',
  ],
  [
    /โดยไม่ต้องอ้างว่ามีหน้าร้านหรือทีมรับถึงที่ในทุกอำเภอ/g,
    'สอบถามพื้นที่ให้บริการและแนวทางนัดรับ/ส่งเครื่องทางไลน์ @webuy ได้ก่อนเดินทาง',
  ],
  [
    /โดยไม่ต้องอ้างว่ามีหน้าร้านหรือทีมรับถึงที่ในทุกพื้นที่ของจังหวัด/g,
    'สอบถามพื้นที่ให้บริการและแนวทางนัดรับ/ส่งเครื่องทางไลน์ @webuy ได้ก่อนเดินทาง',
  ],
  [
    /โดยไม่อ้างว่ามีสาขาหรือทีมประจำในทุกอำเภอ/g,
    'สอบถามพื้นที่ให้บริการและเงื่อนไขการนัดรับ/ส่งเครื่องทางไลน์ @webuy ได้ก่อนเดินทาง',
  ],
  [/โดยไม่อ้างว่ามีสาขาในทุกอำเภอ/g, 'สอบถามพื้นที่ให้บริการทางไลน์ @webuy ได้ก่อนนัด'],
  [
    /โดยไม่อ้างว่ามีทีมรับถึงที่ในทุกตำบล/g,
    'สอบถามพื้นที่ให้บริการและแนวทางส่งเครื่องทางไลน์ @webuy ได้ก่อนนัด',
  ],
  [
    /โดยไม่ต้องอ้างว่ามีทีมรับถึงที่ในทุกตำบล/g,
    'สอบถามพื้นที่ให้บริการและแนวทางส่งเครื่องทางไลน์ @webuy ได้ก่อนนัด',
  ],
  [
    /โดยไม่จำเป็นต้องอ้างว่ามีหน้าร้านหรือทีมรับถึงที่ในจังหวัดเพื่อเริ่มต้นการประเมิน เพราะข้อมูลภาพและรุ่นเป็นหัวใจหลักของการเช็คราคาไอโฟนก่อนขาย/g,
    'การเริ่มจากส่งรูปและแจ้งรุ่น/ความจุผ่านไลน์ @webuy จะช่วยให้ประเมินเบื้องต้นได้ก่อนคุยเรื่องนัดรับหรือส่งเครื่อง',
  ],
  [
    /ไม่ควรตีความเองว่ามีทีมประจำอยู่ในทุกอำเภอ ควรสอบถามพื้นที่ให้บริการและขั้นตอนที่ชัดเจนผ่านไลน์ @webuy หรือโทร 0642579353\n\n/g,
    'หากอยู่ต่างอำเภอ แนะนำสอบถามพื้นที่ให้บริการและขั้นตอนนัดรับ/ส่งเครื่องผ่านไลน์ @webuy หรือโทร 0642579353\n\n',
  ],
  [
    /ไม่ควรตีความเองว่ามีทีมประจำอยู่ในทุกอำเภอ ควรสอบถามพื้นที่ให้บริการและขั้นตอนที่ชัดเจนผ่านไลน์ @webuy หรือโทร 0642579353/g,
    'หากอยู่ต่างอำเภอ แนะนำสอบถามพื้นที่ให้บริการและขั้นตอนนัดรับ/ส่งเครื่องผ่านไลน์ @webuy หรือโทร 0642579353',
  ],
  [/โดยไม่ต้องอ้างว่ามีหน้าร้านในทุกอำเภอ/g, 'สอบถามพื้นที่ให้บริการทางไลน์ @webuy ได้ก่อนนัด'],
  [/โดยไม่ต้องอ้างว่ามีหน้าร้านในทุกตำบล/g, 'สอบถามพื้นที่ให้บริการทางไลน์ @webuy ได้ก่อนนัด'],
  [
    /โดยไม่ต้องอ้างว่ามีทีมรับถึงที่ในทุกจังหวัด/g,
    'สอบถามพื้นที่ให้บริการและวิธีนัดรับ/ส่งเครื่องทางไลน์ @webuy ได้ก่อนตัดสินใจเดินทาง',
  ],
];

let n = 0;
for (const name of fs.readdirSync(locDir)) {
  if (!name.endsWith('.md')) continue;
  const fp = path.join(locDir, name);
  let s = fs.readFileSync(fp, 'utf8');
  const orig = s;
  for (const [re, rep] of pairs) {
    s = s.replace(re, rep);
  }
  if (s !== orig) {
    fs.writeFileSync(fp, s, 'utf8');
    n++;
  }
}
console.log('sanitized', n, 'location markdown file(s)');
